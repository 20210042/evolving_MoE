# Legacy / archival code

Files moved here are not part of the **meta_agent_evolution** core package. They remain for reference or one-off experiments (`main.py`, static persona pools, Jina router training helpers).

To run the modern pipeline, use `scripts/run_evolution.py` and imports from `meta_agent_evo`.
