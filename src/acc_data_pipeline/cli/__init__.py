"""Command-line entry-point package for acc_data_pipeline.

Individual modules implement load, filter, dedup, prepare-execution, and validation steps so each
pipeline stage can be rerun independently."""

